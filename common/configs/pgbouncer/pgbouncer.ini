[databases]
; The legacy route remains available for a bounded rollback path.
polymarket = host=timescaledb-0 port=5432 dbname=polymarket pool_mode=session pool_size=2 max_db_connections=2 max_db_client_connections=6
polymarket_trading = host=timescaledb-0 port=5432 dbname=polymarket pool_mode=session pool_size=8 max_db_connections=8 max_db_client_connections=12
polymarket_ingester_master = host=timescaledb-0 port=5432 dbname=polymarket pool_mode=session pool_size=2 max_db_connections=2 max_db_client_connections=4
polymarket_ingester_worker = host=timescaledb-0 port=5432 dbname=polymarket pool_mode=session pool_size=10 max_db_connections=10 max_db_client_connections=96
polymarket_observe = host=timescaledb-0 port=5432 dbname=polymarket pool_mode=session pool_size=2 max_db_connections=2 max_db_client_connections=6

[pgbouncer]
listen_addr = 0.0.0.0
listen_port = 6432
unix_socket_dir = /tmp
auth_type = scram-sha-256
auth_file = /tmp/pgbouncer/userlist.txt
admin_users = postgres
stats_users = postgres
; Keep session pooling for this identity cutover. In particular, trading SQLx queries using
; persistent(false) require backend affinity across Parse/Sync and Bind/Execute boundaries.
pool_mode = session
max_client_conn = 128
; Per-alias ceilings total 24 PostgreSQL backends, leaving 8 direct slots below the
; PostgreSQL max_connections=32 limit. Dynamic worker clients queue behind their bounded pool.
default_pool_size = 2
max_db_connections = 24
reserve_pool_size = 0
max_prepared_statements = 200
ignore_startup_parameters = extra_float_digits
server_connect_timeout = 5
server_login_retry = 2
server_check_delay = 10
server_tls_sslmode = disable
stats_period = 60
log_connections = 1
log_disconnections = 1
log_pooler_errors = 1
log_stats = 1

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "btc-updown-shadow-ml"
version = "0.1.0"
description = "Execution-isolated shadow ML research for BTC Up/Down markets"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]


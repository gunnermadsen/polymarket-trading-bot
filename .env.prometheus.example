# Generate a unique password and matching bcrypt hash for each environment.
# Do not commit populated .env.prometheus or .env.prometheus.production files.
PROMETHEUS_BASIC_AUTH_USER=
PROMETHEUS_BASIC_AUTH_PASSWORD=
PROMETHEUS_BASIC_AUTH_PASSWORD_HASH=
